# Database Scripts

This directory contains SQL scripts for managing the database schema and data.

## Structure

- Use descriptive names for your SQL files (e.g., `001_initial_schema.sql`, `002_add_users_table.sql`).
- Group scripts by feature or release if necessary.

## Usage

These scripts are intended to be run against your Supabase database to apply changes.
